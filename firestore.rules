rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sooraj-caterers/main {
      allow read, write: if request.auth != null;

      match /events/{id} {
        allow read, write: if request.auth != null;
      }
      match /income/{id} {
        allow read, write: if request.auth != null;
      }
      match /expenses/{id} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
